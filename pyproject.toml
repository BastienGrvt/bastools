[project]
name = "bastools"
version = "0.1.0"
description = ""
authors = [{ name = "bastiengrvt", email = "73183679+BastienGrvt@users.noreply.github.com" }]
readme = "README.md"
requires-python = ">=3.13,<4"
dependencies = [
    "matplotlib>=3.10.3,<4",
    "numpy>=2.3.1,<3",
    "tqdm>=4.67.1,<5",
    "scipy>=1.16.1,<2",
    "rich>=14.1.0,<15",
    "svgwrite>=1.4.3,<2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bastools"]
